# Bookly — standalone migration

Standalone Bookly Android build based on final AppDeploy Bookly snapshot `1787571736295`.

The app is packaged locally as Bookly and does not depend on AppDeploy or a localhost frontend server.

Build verification trigger: 2026-08-24 standalone APK rebuild.
